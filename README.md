# nasp_lua_runtime

This folder is the local files/repository root for the **nasp_lua_runtime** project,
tracked by the planner.

It was created automatically when the project was added to the planner. Task,
step, backlog, and slate data for this project live under the planner's data
directory; this folder holds the project's own files. You can safely add your
own content here.
